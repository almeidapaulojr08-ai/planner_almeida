# NUNCA commitar segredos nem dependências
reports/node_modules/
reports/*serviceAccount*.json
reports/service-account*.json
reports/.env
*.serviceaccount.json
# backups locais (já ficam fora do repo)
index.html.bak
index.html.bak2
backups/

# bot do Telegram (segredos e log locais)
reports/.secrets/
*.log
